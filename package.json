{
  "name": "pocketmock",
  "version": "0.0.21",
  "description": "Mock API server for local development.",
  "main": "pocketmock.js",
  "bin": {
    "pocketmock": "./pocketmock.js"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.3.0",
    "commander": "^2.13.0",
    "fs-extra": "^5.0.0",
    "node-dir": "^0.1.17"
  }
}
